@model VADS.Models.Appointment

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Appointment</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.Maintenance)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Maintenance)
            @Html.ValidationMessageFor(model => model.Maintenance)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Date)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Date)
            @Html.ValidationMessageFor(model => model.Date)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Round)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Round)
            @Html.ValidationMessageFor(model => model.Round)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.AttendantId, "UserProfile")
        </div>
        <div class="editor-field">
            @Html.DropDownList("AttendantId", String.Empty)
            @Html.ValidationMessageFor(model => model.AttendantId)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.VehicleId, "VehicleInfoModel")
        </div>
        <div class="editor-field">
            @Html.DropDownList("VehicleId", String.Empty)
            @Html.ValidationMessageFor(model => model.VehicleId)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
